spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://a96abhinav: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@github.com/a96abhinav/my-cloud-config-repository.git
          default-label: dev
          timeout: 5
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
#      Added below dependency to connect to eureka server in kubernetes containerized env
#      defaultZone: ${EUREKA_URI:http://my-service-registry-app-service:8761/eureka}
  instance:
    preferIpAddress: true

server:
 port: 8888