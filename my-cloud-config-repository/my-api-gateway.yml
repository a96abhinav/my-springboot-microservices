server:
 port: 8081
 servlet.context-path: /${spring.application.name}
spring:
 cloud:
  gateway:
   routes:
    - id: my-springboot-app
      uri: http://127.0.0.1:8085
      predicates:
       - Path=/my-springboot-app/**
