spring:
  application:
    name: my-api-gateway
  config:
#    import: configserver:http://localhost:8888
    import: configserver:http://config-server:8888
  cloud:
   config.label: ${spring.profiles.active}
  profiles:
    active: dev